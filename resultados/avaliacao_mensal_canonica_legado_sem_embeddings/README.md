# Execução incompleta — não publicar

Esta pasta contém somente artefatos parciais de uma tentativa interrompida do
pipeline global opcional. O `status_execucao.json` registra
`fonte_artigos_atuais: false` e uma etapa diferente de `concluido`.

Nenhum CSV, modelo ou número desta árvore pode ser usado no TCC, nos artigos ou
em submissões. A fonte publicável atual permanece exclusivamente:

```text
resultados/avaliacao_mensal_corrigida/
```

Uma futura execução do protocolo global deve usar uma pasta nova e vazia,
concluir todas as etapas e passar pelas validações antes de qualquer análise.
Mesmo concluída, ela constitui um experimento novo e não atualiza
automaticamente os textos atuais.
